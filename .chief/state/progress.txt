# Chief Progress — 107-list-and-ps-show-what-is-live
Started: Fri Aug 21 05:19:24 CDT 2026
---

## ⚠️ PRIOR VERIFICATION FAILED — FIX THESE BEFORE REPORTING COMPLETE
```
doc-links: 4 dead vs 4 on main
verify: quality — ratchet (engine/quality.sh, deltas vs main)
chief quality: ratchet — measuring 10 changed path(s) on HEAD and at main
quality: scope — the 4 changed source file(s), HEAD vs main (2 file(s) new on this branch — no base version, deferred to the baseline axis)
quality:   ok     duplicate_blocks              0 -> 0        (delta 0      tol 0)
quality:   ok     duplicate_line_pct            0 -> 0        (delta 0      tol 1)
quality:   ok     max_nesting_depth             5 -> 5        (delta 0      tol 1)
quality:   ok     function_length_max         269 -> 243      (delta -26    tol 15)
quality:   ok     function_length_mean      21.48 -> 22.24    (delta +0.76  tol 5)
quality:   ok     single_use_functions         30 -> 32       (delta +2     tol 2)
quality:   skip   lint_violations        not measured — no linter configured for this language; shellcheck is not installed
quality: no baseline at .chief/quality-baseline.json — whole-tree ratchet inactive
quality:   (create it once with: chief quality ratchet --write-baseline, then commit it)
quality: OK — no tracked metric regressed (allowing)
verify: shell — bash -n
verify: shellcheck not installed — skipping (CI enforces it)
verify: policy — VERSION bump (test/version-bump.sh)
VERSION-BUMP FAIL: shipped code changed since the last VERSION bump (b88e9fb — VERSION is 0.8.81):
    bin/chief
    engine/monitor.sh
  → bump VERSION so 'chief update' and 'chief version' are distinguishable.
verify: BLOCK — VERSION not bumped for an engine change
```

## Verification follow-up

Resolved the prior merge-time verification failure by correcting six relative documentation links across the plan-review, events, schema, and headless-invocation pages, synchronizing README.md with VERSION 0.8.82, and retaining the existing engine/bin changes under the new version.

Observed verification: documentation link ratchet reports 0 dead references versus 4 on main (4 retired), bash syntax and git diff checks are green. The version and doc-sync gates are run after this commit so their HEAD-based policies can observe the VERSION commit.

Learnings for future iterations: the documentation link checker resolves Markdown links relative to their citing file; cross-directory references must use explicit `../` or directory prefixes, even when the target exists elsewhere under `docs/`.

Final observed checks: `node scripts/check-doc-links.mjs --ratchet --base main` reported 0 dead vs 4 on main; `test/version-bump.sh`, `test/doc-sync.sh`, `test/ps-all.sh`, `test/monitor.sh`, `test/status-deps.sh`, and `test/quality-ratchet.sh` all passed; `bash -n bin/chief engine/*.sh test/*.sh` and `git diff --check` were clean.
