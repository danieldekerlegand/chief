# Chief Progress — 108-concurrency-is-machine-wide
Started: Fri Aug 21 03:18:25 CDT 2026
---

Implemented US-1 in commit b5feedd. Added `engine/concurrency.sh`, which reads the existing cross-repo `.run` registry, reuses `chief_pid_alive` from `reap.sh` (including zombie handling), classifies live tasklist phases as agent turns or gates, and reports stale records without counting them. `chief ps` and the driver now show the machine activity line; `test/concurrency.sh` covers one live run, one agent turn, one gate, and one stale PID. Bumped VERSION/README to 0.8.76.

Verification observed: `test/concurrency.sh`, `bash -n`, `test/version-bump.sh`, `test/doc-sync.sh`, and `test/smoke.sh` passed. `test/ratelimit.sh` remains red on its pre-existing epoch-form fixture (`agent.sh exited 0, want 2`); this story's focused registry test is green.

Learnings for future iterations: the run registry is the single host-wide source of driver liveness; use `chief_pid_alive` rather than raw `kill -0` so zombies do not consume capacity. Keep registry readers non-destructive; `chief ps` owns stale-file pruning.
