# Chief Progress — 110-a-green-branch-should-not-be-verified-three-times
Started: Sat Aug 22 00:31:29 CDT 2026
---

US-1 implemented: added a bounded per-repository verify verdict cache keyed by the
post-rebase tree, base commit, and `.chief/verify.sh` blob. The merge phase records
both green and non-green exits, skips only a matching green record, and reports the
tree plus cache source when skipping. Files changed: engine/lib.sh, engine/driver.sh,
VERSION, README.md. Verification observed the mutation probe green (identical tree
skipped; committed tree and hook mutations missed), bash -n, quality ratchet, doc-sync,
and test/version-bump green; shellcheck was unavailable in this environment.

Learnings for future iterations: cache keys must use `HEAD^{tree}` rather than commit
ids, and the gate script content belongs in the key so a changed verifier cannot reuse
an old verdict. Keep the cache under run state with a per-repository namespace and a
fixed record bound.

Follow-up mutation probe also committed a one-byte tree change and confirmed the
cache missed, then changed the hook and confirmed another miss.

## ⚠️ BASE INTEGRATION REQUIRED — do this FIRST, before any user story

`chief/110-a-green-branch-should-not-be-verified-three-times` is **42 commit(s) behind** its base `main` (@540f9f2) and
chief's automatic rebase onto that base CONFLICTS. The rebase was aborted, so
your branch is exactly as it was and nothing is lost — but it cannot merge
until the base is integrated, and every commit that lands on `main` while
this is unresolved makes it worse.

**Your first task this iteration is to integrate the base yourself:**

1. `git rebase main` in this worktree.
2. At each stop, resolve the conflicts keeping BOTH sides' intent — the
   `main` side is already-merged work, so never discard it, and never
   discard your own — then `git add <files>` and `git rebase --continue`.
   `git rebase --abort` returns you to exactly this state if you need it.
3. Re-run the project's quality gates and make them green.

Rebase, not `git merge main`: chief rebases this branch again at merge time,
and a rebase drops merge commits and replays the originals — a resolution
recorded in a merge commit would be thrown away and this conflict would come
straight back. Resolving inside the rebase puts the fix in your own commits.

Then continue the normal loop: pick the highest-priority story with
`passes:false` and implement it. The integration is NOT a user story — do not
flip any story's `passes` for it, and do not report completion until the
stories themselves are really done.

## Conflicting paths
- README.md
- VERSION

base: main @540f9f2 · behind: 42 commit(s) · branch: chief/110-a-green-branch-should-not-be-verified-three-times
(full note: .chief/state/INTEGRATE-BASE.md)

## US-2 — `chief ps` reports overlap-zone holds honestly

Updated `engine/monitor.sh` so overlap-zone rows explicitly render `awaiting-approval`, retain the elapsed `held` duration, and show the exact `chief approve <name>` action. Approved-but-unmerged holds now say that approval alone does not merge and point to the next `chief run`. Extended `test/stall-flag.sh` with the regression and approved-hold assertions; bumped VERSION/README to 0.8.85.

Learnings for future iterations: monitor rows must derive human-verdict state from the tasklist state/live record rather than the last active phase, and durable approval artifacts need an explicit “not merged yet” message.
