# Chief Progress — 107-list-and-ps-show-what-is-live
Started: Fri Aug 21 05:22:30 CDT 2026
---

## ⚠️ PRIOR VERIFICATION FAILED — FIX THESE BEFORE REPORTING COMPLETE
```
doc-links: 0 dead vs 4 on main
  retired 4
verify: quality — ratchet (engine/quality.sh, deltas vs main)
chief quality: ratchet — measuring 14 changed path(s) on HEAD and at main
quality: scope — the 4 changed source file(s), HEAD vs main (2 file(s) new on this branch — no base version, deferred to the baseline axis)
quality:   ok     duplicate_blocks              0 -> 0        (delta 0      tol 0)
quality:   ok     duplicate_line_pct            0 -> 0        (delta 0      tol 1)
quality:   ok     max_nesting_depth             5 -> 5        (delta 0      tol 1)
quality:   ok     function_length_max         269 -> 243      (delta -26    tol 15)
quality:   ok     function_length_mean      21.48 -> 22.24    (delta +0.76  tol 5)
quality:   ok     single_use_functions         30 -> 32       (delta +2     tol 2)
quality:   skip   lint_violations        not measured — no linter configured for this language; shellcheck is not installed
quality: no baseline at .chief/quality-baseline.json — whole-tree ratchet inactive
quality:   (create it once with: chief quality ratchet --write-baseline, then commit it)
quality: OK — no tracked metric regressed (allowing)
verify: shell — bash -n
verify: shellcheck not installed — skipping (CI enforces it)
verify: policy — VERSION bump (test/version-bump.sh)
VERSION-BUMP PASS — VERSION (0.8.82) covers every engine/bin change on HEAD
verify: behavioral — the hermetic subset, under the bystander guard (test/bystander.sh)
bystander: decoy run up — driver pid 9628, agent pid 9752, run bystander-sibling-865854441-1700-9628
bystander:   its registry is /var/folders/9k/xdzjsfds05j_wfmhs2yr176m0000gp/T/tmp.6smM8tC13F/decoy/runs, which no test below can read
bystander: [1/43] test/smoke.sh
  smoke| SMOKE FAIL: chief list doesn't show the completed smoke tasklist
BYSTANDER FAIL: test/smoke.sh failed (its output is above)
verify: BLOCK — behavioral tests failed (see above)
```

## Verification follow-up resolved

Updated stale tests that still asserted the pre-US-1/pre-US-2 `chief list` output:
the smoke test now uses `list --all` for retired tasklists, park-reasons checks the
STATE/REASON columns, and crossrepo checks the aligned `counterpart merged` reason.
Observed green: `test/all.sh` through the crossrepo boundary after the fixes, plus
the focused smoke, status-deps, park-reasons, crossrepo, doc-sync, version-bump,
`bash -n`, and `git diff --check` checks. Shellcheck is unavailable locally.

### Learnings for future iterations

When a table/default-scope story changes CLI presentation, update older end-to-end
assertions at the same time; legacy tests may validate exact inline markers or
retired rows even though the new behavior is intentional.
