# Chief Progress — 112-a-bookkeeping-commit-is-not-progress
Started: Wed Aug 26 11:52:01 CDT 2026
---

## Codebase Patterns

- **The iteration-outcome classifier lives in ONE block** — `engine/agent.sh`, right after
  `_measure_boundary`/`_prd_promote`: `now_pass=$(_passes); now_head=$(_head)` and the
  if/else that either resets `stall` or increments it. Every "chief scored this iteration
  wrong" bug (111, 112) ends up in that block or in the guards immediately above it
  (rate limit, `_is_no_turn`), which are placed BEFORE it precisely because they are not
  judgements about the work.
- **A behavioural test must REPRODUCE before it is trusted.** The cheap way here: `git clone
  --no-local` this worktree to a temp dir, revert the one line under test in the clone,
  commit, and run the same test file there — the suite installs chief from
  `git rev-parse HEAD` of its own `$ROOT`, so the clone tests the neutered engine with no
  risk to this tree. (`git show HEAD~N` does not work in CI: the clones are shallow.)
- **`cmd | grep -q` under `set -o pipefail` is a length-dependent assertion.** `grep -q`
  exits on the first match and SIGPIPEs the producer, so the pipeline fails when the input
  is long and passes when it is short. Every test in this suite that greps a `git log`
  should capture into a variable first and `case` on it — a fixture whose whole point is
  "the loop ran 20 times instead of 2" will otherwise fail on the wrong assertion.

---

## US-1 — Progress means a story moved or the PRODUCT changed

`engine/agent.sh`:
- `_product_changed PREV NOW` (beside `_passed_ids`) — true when the net tree diff
  `git diff --name-only PREV NOW` touches at least one path outside `$BOOKKEEPING_REL`
  (`${CHIEF_STATE_DIR:-.chief/state}/`). TREE-to-TREE, so an amend/squash landing on an
  identical tree is not progress; and the commit MESSAGE is never read. FAILS OPEN on
  anything it cannot answer (unknown sha, `none`, git error) — the old behaviour was "any
  HEAD move is progress", and an unreadable diff must not become a silent way to stall a
  working run. In a `repo:<sub>` worktree the state dir is not in the tree at all, so no
  path matches the prefix and every commit counts, which is correct there.
- The progress check now reads `[ "$now_pass" -gt "$prev_pass" ] || _product_changed ...`,
  and the stall branch says `no progress — BOOKKEEPING ONLY (...)` when HEAD moved anyway,
  so an operator reading `git log` beside the run does not think chief lost the commit.

`test/bookkeeping-progress.sh` (new; registered in all THREE lists — `.chief/verify.sh`'s
`CHIEF_BYSTANDER_TESTS`, `test/all.sh`'s `BASH_SUITE`, `.github/workflows/ci.yml`):
replays the formant shape — every turn re-stamps `notes` in the runtime prd.json, appends
to progress.txt, `git add -f`s both (chief init gitignores `.chief/state/`) and commits
`feat: US-1 - record the re-check and the blocker`. Asserts the fixture really is
state-only first, then that the run takes exactly `iters` (2) turns.

### Observed
- New test: `BOOKKEEPING PASS` — 2 turns, stall 2/2, INCOMPLETE, nothing merged.
- Reproduced against a neutered clone: **20 turns** (HARD_MAX), log line
  `Iteration 11: progress (0/1 passing). Continuing...` — the formant sentence verbatim.
- `smoke noworkguard ratelimit boundary-demotion evidence-gate measured-bars version-bump
  doc-sync bookkeeping-progress` → 9 passed, 0 failed.
- `.chief/verify.sh` (static half, `CHIEF_VERIFY_TESTS=0`): OK — `bash -n` clean, doc-links
  0 dead vs 0 on main, quality ratchet no regression, VERSION 0.8.95 covers the change.
- `shellcheck` is NOT installed on this host; verify.sh skips it and says so. CI enforces it.

## Learnings for future iterations

- **US-2 has a ready-made home**: add a `bk-both` tasklist to `test/bookkeeping-progress.sh`
  whose agent does real work AND stamps state in the same turn, and assert it scores as
  progress. The fixture, the install and the assertions are already there.
- **US-3's line is `Iteration $i: progress ($now_pass/$(_total) passing). Continuing...`**,
  still exactly as it was — US-1 deliberately left it alone. Note that
  `test/boundary-demotion.sh` greps `Iteration 2: progress` and `Iteration 2: no progress`,
  so keep those two prefixes whatever the rest of the sentence becomes.
- **This repo's own `tasks/chief/*.json` omit `passes` entirely** (the generator writes
  `notes: null` and no flag). `_passes()` counts `passes==true` so the count is right, but
  `_story()` selects `.passes==false` and therefore returns EMPTY for a freshly generated
  tasklist — the liveliness record's `story` field is blank until an agent writes a flag.
  Pre-existing, not touched here, but it is why `chief ps` shows no story id early in a run.
- **`.chief/state/prd.json` and `progress.txt` are TRACKED in chief's own repo** (added
  before `.gitignore` gained `.chief/state/`), so chief self-hosting is exactly the repo
  where the formant pathology can occur — `git log --oneline -- .chief/state` shows
  `chore: driver-seeded run state` commits. That is the case US-2 must keep working.

## US-2 — A legitimate bookkeeping commit is still allowed to happen

`engine/agent.sh`:
- `_touches_bookkeeping` (beside `_product_changed`) — does this tasklist name chief's
  state directory in its `touches`? It changes NOTHING about the verdict. It exists so
  the reclassification is not SILENT: when an iteration is scored BOOKKEEPING ONLY on a
  tasklist that declared the directory, the next line says why the declaration bought
  nothing. `touches` is a CONFLICT DOMAIN the scheduler serializes on (a conceptual tag,
  often not even a path — see `docs/decisions/conflict-predictor-adoption-decision.md`),
  not a scope grant; and a guard the tasklist under judgement can switch off is not a
  guard. That is the whole argument for reporting instead of exempting.
- Nothing was removed. The state paths are still written, committed and merged; they
  just cannot carry the progress verdict alone.

`test/bookkeeping-progress.sh` — the fixture grew a second tasklist, `bk-both`, and the
fake agent now dispatches on `$name` with a shared `stamp()` (both fixtures write the
SAME two state files; the only difference between them is whether anything else moved):
- `bk-both` does real work (`src/step-$turn.txt`) AND stamps `notes`/`progress.txt` in
  ONE commit, and flips its story only on turn 3 — so turns 1-2 can be scored progress by
  nothing except the `src/` path in the diff. That is the exact arm the naive fix breaks.
- Both tasklists declare `touches: [".chief/state"]`: it is the true shared domain (so
  they serialize instead of tripping the under-tagged-touches audit, which was RIGHT),
  and it puts them in the position criterion 3 asks about — so the run also asserts the
  notice is printed.
- Asserts the merged tree still carries the notes and the progress line, and the retired
  `completed/bk-both.json` still carries `notes`.

`docs/reference/tasklist-schema.md` — new bullet before the crossRepo one: progress is
judged on the DIFF; `.chief/state/**`-only is bookkeeping; notes stay supported; work +
notes in one commit scores on the work; `touches` cannot exempt; and the honest
admission — **work whose entire product is `.chief/state/` is not a tasklist chief can
drive to completion**, because every iteration of it scores as a stall.

### Observed
- `bookkeeping-progress`: PASS — bk-spin 2 turns / stall 2/2 / INCOMPLETE, bk-both **3
  turns past its 2-iter budget → MERGED**, `Iteration 1: progress (0/1 passing)`, no
  BOOKKEEPING ONLY in its log, notes + progress.txt on main.
- REPRODUCED, twice, in `git clone --no-local` copies with the engine neutered:
  the naive fix (`case "$f" in "$BOOKKEEPING_REL"*) return 1`) → *"the pairing case took
  2 turns against a 2-iter budget"*; dropping the notice → *"reclassified as bookkeeping
  SILENTLY"*.
- `version-bump doc-sync boundary-demotion evidence-gate measured-bars noworkguard
  ratelimit smoke` → 8 PASS, 0 FAIL.
- `.chief/verify.sh` (static half): OK — `bash -n` clean, doc-links 0 dead vs 0 on main,
  quality ratchet no regression (`single_use_functions 14 -> 16`, tol 2 — AT the
  tolerance, so the next story adding two helpers to a changed file will trip it),
  VERSION 0.8.96, DOC-SYNC PASS.
- `shellcheck` still not installed on this host; verify skips it, CI enforces it.

## Learnings for future iterations

- **The quality ratchet is at its `single_use_functions` tolerance (+2 of 2).** US-3 adds
  output, not helpers, if it can — or it should inline rather than extract.
- **US-3's remaining surface, now that the two verdict lines are settled:** the progress
  line is still `Iteration $i: progress ($now_pass/$(_total) passing). Continuing...` and
  criterion 1 says that sentence must not be printable — but note `bk-both` legitimately
  prints `progress (0/1 passing)` and MERGES three turns later, so "0 passing" is not by
  itself the bug. What made formant's line a lie was `progress` over a state-only diff,
  which no longer happens; what is left is to say WHAT advanced. Any rewording must keep
  the prefixes `Iteration N: progress` and `Iteration N: no progress`
  (`test/boundary-demotion.sh` and now `test/bookkeeping-progress.sh` both grep them).
- **A second tasklist in a fixture repo moves `main` under the first one.** `git diff
  main "$BR"` then reports the sibling's merged files as deletions on your branch — diff
  against `git merge-base main "$BR"` instead. Same for a `git log` range.
- **A fixture whose tasklists write the same files needs a shared `touches`**, or the
  run ends with a legitimate under-tagged-touches warning and the two workers race on a
  rebase. Sharing the domain serializes them and is the honest tag.
