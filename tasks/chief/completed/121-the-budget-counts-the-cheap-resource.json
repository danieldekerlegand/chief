{
  "project": "chief",
  "category": "fix",
  "branchName": "chief/121-the-budget-counts-the-cheap-resource",
  "dependsOn": [],
  "touches": [
    "engine/concurrency.sh",
    "engine/driver.sh",
    "engine/monitor.sh",
    "test",
    "docs",
    "VERSION"
  ],
  "iters": 8,
  "warmup": [
    "sed -n '1,60p' engine/concurrency.sh",
    "sed -n '/^chief_machine_budget_allows()/,/^}/p' engine/concurrency.sh",
    "sed -n '/^chief_machine_load_line()/,/^}/p' engine/concurrency.sh",
    "grep -n 'CHIEF_MACHINE_GATES\\|CHIEF_MACHINE_LOAD_AVERAGE' engine/",
    "sed -n '3325,3360p' engine/driver.sh",
    "sed -n '/jobs = 7/,/^$/p' ~/Development/cuneiform/core/engine/.cargo/config.toml"
  ],
  "description": "The machine budget governs AGENT TURNS against a ceiling derived from CPU cores, counts gates without ever consulting the count, and reads load average only to print a word. So a host can be measurably oversubscribed while the scheduler believes it has two thirds of its capacity free. Observed 2026-09-03.\n\nWHAT WAS OBSERVED. `chief ps`, five repos running `-p1` each:\n\n    Machine activity: 5 live run(s) · 5 agent turn(s) · 0 gate(s) ·\n                      load average: 14.32 / 14 physical core(s) · OVERSUBSCRIBED\n\nThe budget defaults to the physical core count, so it was 14. Five agent turns were live. `chief_machine_budget_allows` was therefore true, and the launch loop would have admitted NINE MORE runs onto a box already past its core count.\n\nTHE THREE HALVES OF THE DEFECT.\n\n1. WRONG UNIT. `chief_machine_budget_allows` is\n\n       [ \"$CHIEF_MACHINE_BUDGET_DISABLED\" = 1 ] || \\\n       [ \"$CHIEF_MACHINE_AGENT_TURNS\" -lt \"$CHIEF_MACHINE_BUDGET\" ]\n\n   and `chief_machine_budget_init` sets the budget to `chief_machine_core_count`. An agent turn is dominated by `provider-waiting` — network latency, not compute; the engine's own phase table says so by giving it a 900s staleness threshold. A GATE is the compute. So the budget limits the cheap resource using a number derived from the expensive one.\n\n2. THE EXPENSIVE HALF IS COUNTED AND IGNORED. `CHIEF_MACHINE_GATES` is incremented at `concurrency.sh:162` and printed at `:170`. `grep -rn CHIEF_MACHINE_GATES engine/ bin/` returns those two plus two initialisations, and nothing else. The number the scheduler most needs is already computed and is used only for display.\n\n3. LOAD AVERAGE IS DECORATION. `CHIEF_MACHINE_LOAD_AVERAGE` is written at `concurrency.sh:88` and read at `:86`, both inside `chief_machine_load_line`. `OVERSUBSCRIBED` is a string appended next to a number and that is the whole of its effect.\n\nPREVENTION WITHOUT RELIEF. The budget check sits in the launch `while` loop in `driver.sh`, whose comment states the limit plainly: 'Workers already running keep going.' That is correct for an operator pause, and wrong for a machine-capacity hold — five workers whose gates happen to land together cannot be spread out after the fact, because the only decision point has already passed.\n\nWHY THIS COSTS REAL TIME. cuneiform caps cargo at `jobs = 7` with its reasoning written down: '7 is half the physical cores, chosen so TWO concurrent gates exactly saturate the machine rather than oversubscribing it 2x.' That reasoning is sound and it is defeated from OUTSIDE the repo, because nothing stops a third, fourth and fifth gate starting in other repos. A per-repo contention control cannot hold a line that is crossed host-wide.\n\nSCOPE. This changes ADMISSION only. It must not kill, suspend or restart a running gate: a gate interrupted mid-run is a corrupt verdict and a wasted rebuild, which is worse than the contention. The lever is when a worker is ALLOWED TO BEGIN one.",
  "userStories": [
    {
      "id": "US-1",
      "title": "Gates are budgeted as their own resource",
      "passes": true,
      "notes": "Added CHIEF_MACHINE_GATE_BUDGET (default 2, argued in engine/concurrency.sh against cuneiform's jobs=7 = cores/2, which saturates at 2 gates at any core count) as a budget separate from CHIEF_MACHINE_BUDGET; both env overrides and both 0|off|false|none escape hatches verified independent. Enforced by chief_machine_gate_admit at the two places a gate BEGINS (driver.sh merge phase after the merge lock; agent.sh _agent_verify_final on a cache miss), never by interrupting a running gate. Reads the existing CHIEF_MACHINE_GATES counter — no second state store. Two no-deadlock floors (zero gates live always admits; CHIEF_MACHINE_GATE_HOLD_MAX bounds any hold). test/gate-budget.sh: PASS, 6 parts (A-F) green, including the zero-headroom case (budget 0, idle host, admitted in 0s) and the stuck-gate bound; mutation-checked (restoring merge-wait to the gate kind fails part B). test/machine-budget.sh: PASS (was RED before this change on any >1-core host). bash -n clean over bin/chief engine/*.sh install.sh test/*.sh templates/verify.sh; quality ratchet: allowing.",
      "acceptanceCriteria": [
        "A gate budget exists separately from the agent-turn budget and is enforced before a worker ENTERS its gate phase, not only at run launch. It reads the `CHIEF_MACHINE_GATES` count that `chief_machine_activity` already maintains — this story consults an existing counter rather than adding a second state store, which the concurrency reader's header explicitly forbids.",
        "The default is justified in a comment against the machine, not chosen by feel. cuneiform's `jobs = 7` assumes two concurrent gates saturate a 14-core host; whatever default is chosen must state its relationship to the core count and to that assumption.",
        "IT CANNOT DEADLOCK. At least one gate is always admitted, however high the count or the load. A test drives the budget to zero-headroom and asserts a gate still starts. A scheduler that can refuse every gate stops the portfolio permanently, which is a worse failure than the contention this fixes.",
        "The agent-turn budget keeps working as it does now, with its existing `CHIEF_MACHINE_BUDGET` env override and its `0|off|false|none` escape hatch intact. Its default may change only if the change is argued for in the same comment; the two budgets are separate knobs and this story must not collapse them into one."
      ],
      "diffSize": {
        "files": 16,
        "lines": 400,
        "renames": 0,
        "budget": {
          "lines": 400,
          "files": 20
        },
        "overBudget": false
      }
    },
    {
      "id": "US-2",
      "title": "Load average is an input, not decoration",
      "passes": true,
      "notes": "Load average is now an ADMISSION input at both admission points, not a printed word. CHIEF_MACHINE_LOAD_LIMIT (default = physical core count, the same line the OVERSUBSCRIBED display already drew; 0|off|false|none disables, resolved in chief_machine_budget_init beside the other two knobs and exported from bin/chief + documented in .chief/config and docs/reference/concurrency.md). ONE sampler owns the reading: chief_machine_load_sample writes CHIEF_MACHINE_LOAD_AVERAGE and chief_machine_load_line, chief_machine_load_allows and the hold reason all read that global. BOUNDED, not absolute: chief defers to the load line only while a gate of chief's is in flight, so foreign load with chief idle admits immediately (the same zero-gates floor the gate budget stands on), and a hold stays bounded by CHIEF_MACHINE_GATE_HOLD_MAX on top of that. test/load-admission.sh: PASS, 6 parts (A-F) green, including the simulated load 62 / ceiling 4 with ZERO live runs admitting in 0s; MUTATION-CHECKED three ways (load predicate always-admits fails B, removing the idle-host floor fails C, restoring the gate-budget-only early return in chief_machine_gate_admit fails F). test/gate-budget.sh PASS, test/machine-budget.sh PASS, test/concurrency.sh PASS, test/concurrency-monitor.sh PASS, test/stall-flag.sh PASS. bash -n clean over bin/chief engine/*.sh install.sh test/*.sh templates/verify.sh .chief/verify.sh; shellcheck 0.11.0 -S error clean (via npx) on the four changed shell files; ci.yml parses under yaml.safe_load; check-doc-links 0 dead references; quality ratchet OK (function_length_max 602 -> 613 tol 15, single_use_functions 61 -> 63 tol 2, duplicate_blocks 0 -> 0). VERSION 0.9.20 -> 0.9.21 with README and ROADMAP bumped in the same commit.",
      "acceptanceCriteria": [
        "Admission consults `CHIEF_MACHINE_LOAD_AVERAGE`. A host already past its core count does not admit more work, whether the load came from chief or from anything else on the box.",
        "IT CANNOT STALL ON FOREIGN LOAD. Load average includes work chief did not start — an operator's own build, a browser, a VM. A rule that waits for load to fall can therefore wait forever while chief itself is idle. Bound it: when nothing of chief's is running, work is admitted regardless of load. A test asserts this with a simulated high load and zero live runs.",
        "The load reading is fed in, not re-derived at each call site — one function owns it, as `chief_machine_load_line` does today, and callers read the global. A second sampling path is how the display and the decision drift apart.",
        "An env override disables load-based admission entirely, in the same style and documented in the same place as `CHIEF_MACHINE_BUDGET`. An operator who knows the load is foreign must be able to say so."
      ],
      "diffSize": {
        "files": 14,
        "lines": 381,
        "renames": 0,
        "budget": {
          "lines": 400,
          "files": 20
        },
        "overBudget": false
      }
    },
    {
      "id": "US-3",
      "title": "The display stops overstating headroom",
      "passes": true,
      "notes": "`chief ps` now reports both budgets and the load ceiling, and a HEADROOM line that is the ADMISSION answer rather than the subtraction (a refusing control reports 0 for both budgets and names itself). OVERSUBSCRIBED is reserved for a ceiling that actually refused a gate; over the ceiling with chief idle reads `over ceiling — admitting, no chief gate in flight`. The gate hold gets its own row (⏸ gate-hold, phase gate-budget-waiting) beside the launch hold's budget-hold, and the note is chief_machine_admits' own reason string, so a display can no longer name a resource other than the one that refused. docs/reference/concurrency.md gains 'The model, in one place' (three controls, why turns and gates are budgeted apart, why load is bounded, and the scope line that leaves per-repo cargo/npm settings alone). test/concurrency-monitor.sh rewritten (4 parts) and promoted into all three gate lists — it was in test/all.sh only and passed only on a 14-core host. OBSERVED: test/concurrency-monitor.sh PASS, MUTATION-CHECKED four ways (unconditional OVERSUBSCRIBED fails A; plain-subtraction headroom fails B; removing the gate-budget-waiting arm fails C; the old fixed note string fails C). concurrency PASS, machine-budget PASS, gate-budget PASS, load-admission PASS, stall-flag PASS, doc-sync PASS, version-bump PASS. bash -n clean over bin/chief engine/*.sh install.sh test/*.sh templates/verify.sh .chief/verify.sh; shellcheck 0.11.0 -S error clean (npx) on the 5 changed shell files; ci.yml parses under yaml.safe_load; check-doc-links 0 dead references; quality ratchet OK (single_use_functions 83 -> 84 tol 2, function_length_max 602 -> 613 tol 15, duplicate_blocks 0 -> 0). VERSION 0.9.21 -> 0.9.22 with README and ROADMAP bumped in the same commit. Live `chief ps` on the real host: '3 live run(s) · 2/14 agent turn(s) · 1/2 gate(s) · load average: 12.36 / 14 physical core(s)' + 'headroom: 12 agent turn(s) · 1 gate(s)'.",
      "acceptanceCriteria": [
        "`chief ps` reports both budgets and the headroom actually remaining, so `0 gate(s)` alongside a saturated load average can no longer read as an idle machine. `OVERSUBSCRIBED` appears only where it now means something happened.",
        "A worker held at the gate boundary is distinguishable from one blocked on dependencies and from one merely pending — the launch loop already sets `phase=machine-budget-waiting` for the agent-turn hold, and the gate hold gets the same treatment, with the reason and the counts written to the `machine-budget` log the way the existing hold does.",
        "`docs/` gains one short section stating the model: which resource each budget governs, why an agent turn and a gate are budgeted separately, and why load average is bounded rather than absolute. This is the part a future reader needs in order not to re-collapse the two budgets.",
        "The `340`-era per-repo contention controls are left alone. This tasklist adds the host-wide line that cuneiform's `jobs = 7` comment assumes exists; it does not modify any repo's own cargo, npm or gate configuration."
      ],
      "diffSize": {
        "files": 13,
        "lines": 479,
        "renames": 0,
        "budget": {
          "lines": 400,
          "files": 20
        },
        "overBudget": true
      }
    }
  ],
  "mergedToMain": "57d0e4b",
  "gates": {
    "local": {
      "state": "passed",
      "detail": "the project verify hook (verify.sh) ran and exited 0"
    },
    "ci": [
      {
        "workflow": "ci",
        "state": "unknown",
        "detail": "a push of main can start it; whether it RAN for 57d0e4b is not recorded here — chief makes no network call on the merge path. `chief cigate` is what asks GitHub."
      }
    ]
  }
}
