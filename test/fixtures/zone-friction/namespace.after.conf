# the same file, narrowed to the one surface CLAUDE.md already calls contended:
# the agent exit-code namespace, which parallel tasklists claim numbers out of
review  surface:engine/driver.sh:^AGENT_RC_   the contended exit-code namespace, not the rest of the driver
